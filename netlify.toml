[build]
  command = "npm install --prefix ./netlify/functions && ng build"
  publish = "dist/browser"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
  included_files = ["./netlify/functions/templates/**"]